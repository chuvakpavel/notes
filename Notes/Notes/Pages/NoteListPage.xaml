<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Notes.ViewModels;assembly=Notes"
             x:Class="Notes.Pages.NoteListPage" Title="Notes">
    <ContentPage.BindingContext>
        <viewModels:NoteListViewModel x:Name="ViewModel"/>
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="AddNoteClicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ListView ItemsSource="{Binding NoteList}" ItemSelected="NoteSelected" SelectedItem="{Binding SelectedItem}">
            <ListView.ItemTemplate>
             <DataTemplate>
                    <TextCell Text="{Binding Title}" Detail="{Binding Description}" >
                        <TextCell.ContextActions>
                            <MenuItem Clicked="DeleteClicked" CommandParameter="{Binding .}"
                                      Text="Delete" IsDestructive="True" />
                        </TextCell.ContextActions>
                    </TextCell>
             </DataTemplate>
         </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>